apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage
  namespace: homepage
spec:
  secretName: saturn.ong-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - saturn.ong