apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-misc
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: argocd
    path: kubernetes/infra/misc/apps

  destination:
    name: in-cluster
    namespace: default

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: false
      selfHeal: true

  