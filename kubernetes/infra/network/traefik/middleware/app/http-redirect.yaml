apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: http-redirect
  namespace: traefik
spec:
  redirectRegex:
    regex: ^http://(?!.*\/\.well-known\/acme-challenge\/)(.*)
    replacement: https://${1}