apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap
  namespace: auth
spec:
  secretName: ldap.saturn.ci
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - ldap.saturn.ci