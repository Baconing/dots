apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authelia
  namespace: auth
spec:
  secretName: sso.saturn.ci
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - sso.saturn.ci