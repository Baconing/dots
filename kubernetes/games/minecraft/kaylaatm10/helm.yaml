apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kaylaatm10
  namespace: minecraft
spec:
  interval: 5m
  chart:
    spec:
      chart: minecraft
      version: 5.1.3
      sourceRef:
        kind: HelmRepository
        name: itzg
        namespace: flux-system
  values:
    image:
      repository: itzg/minecraft-server
      tag: 2026.7.0-java21-jdk
      pullPolicy: IfNotPresent

    resources:
      requests:
        cpu: 1
        memory: 2048Mi
      limits:
        cpu: 4
        memory: 12288Mi

    extraEnv:
      INIT_MEMORY: 2048M
      MAX_MEMORY: 12288M

    envFrom:
      - secretRef:
          name: curseforge-api-token

    livenessProbe:
      initialDelaySeconds: 300 # 5 minutes

    readynessProbe:
      initialDelaySeconds: 300 # 5 minutes

    workloadAsStatefulSet: true
    strategyType: OnDelete

    minecraftServer:
      eula: "TRUE"
      type: AUTO_CURSEFORGE
      difficulty: easy
      whitelist: "Baconing,TickleKnees"
      ops: "Baconing"
      enableCommandBlock: true
      spawnProtection: -1

      motd: "i love you so os sososo so soso so sos o much meow"

      autoCurseForge:
        apiKey:
          existingSecret: curseforge-api-token
        slug: all-the-mods-10
        fileId: "8323938"

    persistence:
      storageClass: longhorn
      dataDir:
        enabled: true
        Size: 16Gi
        accessModes:
          - ReadWriteOnce

    serviceAnnotations:
      mc-router.itzg.me/externalServerName: kaylaatm10.saturn.ci,kaylaatm10.local
      external-dns.alpha.kubernetes.io/enabled: "true"
      external-dns.alpha.kubernetes.io/hostname: kaylaatm10.saturn.ci
      external-dns.alpha.kubernetes.io/target: direct.saturn.ci
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"