apiVersion: v1
kind: Service
metadata:
  name: cross-seed
  namespace: media
spec:
  selector:
    app: cross-seed
  ports:
    - port: 2468
      targetPort: 2468
      protocol: TCP
      name: http