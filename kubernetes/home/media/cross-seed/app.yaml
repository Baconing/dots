apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cross-seed
  namespace: argocd
spec:
  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: argocd
    path: kubernetes/home/media/cross-seed

  destination:
    server: in-cluster
    namespace: media

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: false
      selfHeal: true

  plugin: {}