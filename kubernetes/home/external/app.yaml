apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-external
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: master
    path: kubernetes/home/external/app

  destination:
    name: in-cluster
    namespace: external

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: true
      selfHeal: true

  