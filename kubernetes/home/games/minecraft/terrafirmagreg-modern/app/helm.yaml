apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: minecraft-terrafermagreg-modern-helm
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    chart: minecraft
    repoURL: https://itzg.github.io/minecraft-server-charts/
    targetRevision: 5.1.3
    helm:
      releaseName: terrafermagreg-modern
      valuesObject:
        image:
          repository: itzg/minecraft-server
          tag: 2026.9.1-java21-jdk
          pullPolicy: IfNotPresent

        resources:
          requests:
            cpu: 1
            memory: 4096Mi
          limits:
            cpu: 4
            memory: 18432Mi

        extraEnv:
          INIT_MEMORY: 4096M
          MAX_MEMORY: 16384M
          
          GENERIC_PACK: "https://github.com/TerraFirmaGreg-Team/Modpack-Modern/releases/download/0.13.10/TerraFirmaGreg-Modern-0.13.10-serverpack.zip"

        livenessProbe:
          initialDelaySeconds: 300 # 5 minutes

        workloadAsStatefulSet: true
        strategyType: OnDelete

        minecraftServer:
          eula: "TRUE"
          type: FORGE
          version: 1.20.1
          forgeVersion: 47.4.13
          whitelist: "Baconing,TickleKnees,Kamieo_"
          ops: "Baconing"
          overrideServerProperties: false
          
          modUrls:
            - "https://cdn.modrinth.com/data/1s5x833P/versions/7Pn5jnNB/neruina-3.3.3%2B1.20.1-forge.jar?mr_download_reason=standalone&mr_game_version=1.20.1&mr_loader=forge"

        persistence:
          labels:
            recurring-job.longhorn.io/source: enabled
            recurring-job-group.longhorn.io/backup: enabled
          storageClass: longhorn
          dataDir:
            enabled: true
            Size: 16Gi
            accessModes:
              - ReadWriteOnce

        serviceAnnotations:
          mc-router.itzg.me/externalServerName: tfgm.saturn.ci,tfgm.local
          external-dns.alpha.kubernetes.io/enabled: "true"
          external-dns.alpha.kubernetes.io/hostname: tfgm.saturn.ci
          external-dns.alpha.kubernetes.io/target: direct.saturn.ci
          external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"

  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /spec/replicas

  destination:
    name: in-cluster
    namespace: minecraft

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
      - RespectIgnoreDifferences=true
    automated:
      prune: true
      selfHeal: true

  