apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: archisteamfarm
  namespace: argocd
spec:
  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: master
    path: kubernetes/home/games/archisteamfarm

  destination:
    server: in-cluster
    namespace: archisteamfarm

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true