apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: beammp
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: argocd
    path: kubernetes/home/games/beammp

  destination:
    name: in-cluster
    namespace: beammp

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: false
      selfHeal: true

  plugin: {}