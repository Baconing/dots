apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: printer-ustreamer
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: argocd
    path: kubernetes/home/printer/ustreamer/app

  destination:
    name: in-cluster
    namespace: printer

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: true
      selfHeal: true

  