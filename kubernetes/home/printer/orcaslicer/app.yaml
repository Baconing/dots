apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: printer-orcaslicer
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: master
    path: kubernetes/home/printer/orcaslicer/app

  destination:
    name: in-cluster
    namespace: printer

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: true
      selfHeal: true

  