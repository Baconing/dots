apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: printer-mainsail
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/baconing/dots.git
    targetRevision: master
    path: kubernetes/home/printer/mainsail/app

  destination:
    name: in-cluster
    namespace: printer

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - FailOnSharedResource=true
    automated:
      prune: true
      selfHeal: true

  