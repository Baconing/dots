apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: router
  namespace: external
spec:
  secretName: router.saturn.ci
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - router.saturn.ci