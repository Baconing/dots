apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-assistant
  namespace: external
spec:
  secretName: hass.saturn.ci
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - hass.saturn.ci