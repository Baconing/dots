apiVersion: v1
kind: Service
metadata:
  name: &appName spoolman
  namespace: printer
spec:
  selector:
    app: *appName
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
