apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &appName orcaslicer
  namespace: printer
  annotations:
    kubernetes.io/ingress.class: traefik
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: OrcaSlicer
    gethomepage.dev/group: Printer
    gethomepage.dev/icon: orcaslicer
    traefik.ingress.kubernetes.io/router.tls: "true"
    external-dns.alpha.kubernetes.io/enabled: "true"
    external-dns.alpha.kubernetes.io/hostname: &host orcaslicer.saturn.ci
    external-dns.alpha.kubernetes.io/target: direct.saturn.ci
spec:
  rules:
    - host: *host
      http:
        paths:
        - pathType: Prefix
          path: "/"
          backend:
            service:
              name: *appName
              port: 
                name: http
  tls:
    - secretName: *host
      hosts:
        - *host
