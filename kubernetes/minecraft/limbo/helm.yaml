apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: limbo
  namespace: minecraft
spec:
  interval: 5m
  chart:
    spec:
      chart: minecraft
      version: 5.1.2
      sourceRef:
        kind: HelmRepository
        name: itzg
        namespace: flux-system
  values:
    image:
      repository: itzg/minecraft-server
      tag: 2026.5.0-java21-jdk
      pullPolicy: IfNotPresent

    resources:
      requests:
        cpu: 16m
        memory: 64Mi
      limits:
        cpu: 256m
        memory: 256Mi

    minecraftServer:
      eula: "TRUE"
      type: LIMBO
      LIMBO_BUILD: latest

    persistence:
      dataDir:
        enabled: false

    serviceAnnotations:
      mc-router.itzg.me/defaultServer: "true"