apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert
  namespace: flux-system
spec:
  interval: 1m
  path: ./kubernetes/cert
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system