apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metallb
  namespace: flux-system
spec:
  interval: 1m
  path: ./kubernetes/metallb
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system