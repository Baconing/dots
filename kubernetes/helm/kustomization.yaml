apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: helm
  namespace: flux-system
spec:
  interval: 5m
  path: kubernetes/helm
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system