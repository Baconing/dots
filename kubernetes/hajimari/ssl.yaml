apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hajimari
  namespace: hajimari
spec:
  secretName: saturn.ong-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - saturn.ong