apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgadmin
  namespace: database
spec:
  secretName: pgadmin.saturn.ong-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - pgadmin.saturn.ong