apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgadmin
  namespace: database
spec:
  secretName: pgadmin.saturn.ci
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - pgadmin.saturn.ci